{
  "kind": "collectionType",
  "collectionName": "tests",
  "info": {
    "name": "test"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "enum": {
      "type": "enumeration",
      "enum": [
        "Ola",
        "Adeus"
      ]
    }
  }
}
