{
  "kind": "collectionType",
  "collectionName": "free_bets",
  "info": {
    "name": "free-bets"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "type": {
      "type": "enumeration",
      "enum": [
        "Simples",
        "Combinada"
      ]
    },
    "risk": {
      "type": "enumeration",
      "enum": [
        "Valor",
        "Garantida"
      ]
    },
    "match": {
      "type": "component",
      "repeatable": true,
      "component": "match.match"
    }
  }
}
