{
  "kind": "collectionType",
  "collectionName": "free_bets",
  "info": {
    "name": "free-bets"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "type": {
      "type": "enumeration",
      "enum": [
        "Simples",
        "Combinada"
      ]
    },
    "match": {
      "type": "component",
      "repeatable": true,
      "component": "matches.match"
    },
    "risk": {
      "type": "enumeration",
      "enum": [
        "Valor",
        "Garantida"
      ]
    }
  }
}
